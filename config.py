# -*- coding: utf-8 -*-
"""
تنظیمات اولیهٔ ربات ثبت‌نام تورنمنت Mobile Legends.

همهٔ مقادیر قابل‌تغییر و حساس، طبق درخواست، همین‌جا در ابتدای پروژه
جمع شده‌اند تا نیازی به گشتن در کل کد نباشد.
"""
import os
# ─────────────────────────────────────────────────────────────
# توکن ربات (از @BotFather دریافت می‌شود)
# ─────────────────────────────────────────────────────────────
BOT_TOKEN = os.environ["BOT_TOKEN"]

# ─────────────────────────────────────────────────────────────
# شناسهٔ عددی گروه خصوصی ادمین
# ربات باید در این گروه عضو و ادمین باشد (برای ارسال/ویرایش/حذف پیام).
# برای گرفتن شناسه: ربات را به گروه اضافه کنید، یک پیام در گروه بفرستید
# و با @RawDataBot یا متد getUpdates شناسهٔ گروه (معمولاً منفی) را بگیرید.
# ─────────────────────────────────────────────────────────────
ADMIN_GROUP_ID = int(os.environ["ADMIN_GROUP_ID"])

# ─────────────────────────────────────────────────────────────
# فهرست شناسهٔ عددی ادمین‌هایی که مجاز به تأیید/رد ثبت‌نام هستند
# (در حال حاضر فقط برای مستندسازی/توسعه‌های بعدی نگه داشته می‌شود؛
#  بررسی اصلی از طریق دکمه‌های داخل گروه ادمین انجام می‌شود)
# ─────────────────────────────────────────────────────────────
ADMIN_IDS = [5968015463]

# ─────────────────────────────────────────────────────────────
# هزینه‌ها (به تومان یا هر واحد دلخواه)
# ─────────────────────────────────────────────────────────────
BASE_COST = 800000            # هزینهٔ پایه (تا ۴ عضو)
EXTRA_PLAYER_COST = 20_000      # هزینهٔ هر عضو اضافه بر ۴ نفر

# ─────────────────────────────────────────────────────────────
# زمان مجاز برای تکمیل ثبت‌نام (بر حسب ثانیه) — پیش‌فرض ۵۰ دقیقه
# ─────────────────────────────────────────────────────────────
REGISTRATION_TIMEOUT_SECONDS = 50 * 60

# ─────────────────────────────────────────────────────────────
# شناسهٔ (یوزرنیم) اکانت پشتیبانی برای پیگیری ثبت‌نام‌های ردشده
# ─────────────────────────────────────────────────────────────
SUPPORT_USERNAME = "@id4k0"

# ─────────────────────────────────────────────────────────────
# حداقل و حداکثر تعداد اعضای اسکواد (لیدر جزو این تعداد نیست)
# ─────────────────────────────────────────────────────────────
MIN_MEMBERS = 4
MAX_MEMBERS = 9

# ─────────────────────────────────────────────────────────────
# صف ارسال پیام به گروه ادمین — فاصلهٔ زمانی بین دو ارسال متوالی
# (برای جلوگیری از خطای Flood Control تلگرام)
# ─────────────────────────────────────────────────────────────
QUEUE_MIN_DELAY = 0.05   # ثانیه
QUEUE_MAX_DELAY = 0.10   # ثانیه

# ─────────────────────────────────────────────────────────────
# فایل PicklePersistence — وضعیت conversation و ایندکس داخلی
# (نام‌های رزروشده/شماره‌تلفن‌ها و اطلاعات هر اسکواد) روی دیسک
# در همین فایل نگه‌داری می‌شود تا با ری‌استارت ربات از بین نرود.
# ─────────────────────────────────────────────────────────────
PERSISTENCE_FILE = "bot_persistence.pickle"

# هر چند ثانیه یک‌بار PicklePersistence تغییرات را روی دیسک بنویسد.
# عدد کوچک = ایمنی بیشتر در برابر ری‌استارت ناگهانی، هزینهٔ IO بیشتر.
PERSISTENCE_UPDATE_INTERVAL = 1

LOG_LEVEL = "INFO"
